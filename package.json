{
  "name": "lint-config",
  "workspaces": [
    "create-lint-config",
    "eslint/*",
    "generator-lint-config",
    "prettier-config"
  ],
  "scripts": {
    "lint": "eslint .",
    "test": "jest",
    "release": "lerna publish --conventional-commits",
    "postinstall": "husky install"
  },
  "private": true,
  "devDependencies": {
    "@commitlint/cli": "^12.0.0",
    "@commitlint/config-conventional": "^11.0.0",
    "husky": "^5.1.0",
    "jest": "^26.6.3",
    "lerna": "^3.22.1",
    "lint-staged": "^10.5.4"
  },
  "resolutions": {
    "**/yeoman-test/yeoman-environment": "^3.0.0",
    "**/yeoman-test/yeoman-generator": "^5.0.0"
  },
  "prettier": "./prettier-config",
  "lint-staged": {
    "*.js": "eslint --fix",
    "*.{json,md,yml}": "prettier --write"
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ],
    "rules": {
      "header-max-length": [
        0
      ]
    }
  },
  "jest": {
    "testEnvironment": "node",
    "testPathIgnorePatterns": [
      "fixtures"
    ]
  },
  "volta": {
    "node": "12.16.1",
    "yarn": "1.22.4"
  }
}
